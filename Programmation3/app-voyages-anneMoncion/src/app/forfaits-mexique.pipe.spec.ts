import { ForfaitsMexiquePipe } from './forfaits-mexique.pipe';

describe('ForfaitsMexiquePipe', () => {
  it('create an instance', () => {
    const pipe = new ForfaitsMexiquePipe();
    expect(pipe).toBeTruthy();
  });
});
